async function profile(){shell('Profile');let u=await api('/auth/me');content.innerHTML=`<form class="card" id="profile"><label>First name</label><input name="first_name" value="${u.first_name}"><label>Last name</label><input name="last_name" value="${u.last_name}"><label>Username</label><input name="username" value="${u.username}"><label>Email</label><input name="email" value="${u.email}"><label>Education level</label><input name="education_level" value="${u.education_level||''}"><p><button>Save profile</button></p></form>`;profile.onsubmit=async e=>{e.preventDefault();await api('/users/me',{method:'PUT',body:JSON.stringify(Object.fromEntries(new FormData(profile)))});toast('Profile saved')}}document.addEventListener('DOMContentLoaded',()=>{if(document.body.dataset.page==='profile')profile().catch(x=>toast(x.message))})
