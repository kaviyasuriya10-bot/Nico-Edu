document.addEventListener('DOMContentLoaded',()=>{if(document.body.dataset.page!=='settings')return;shell('Settings');content.innerHTML=`<section class="card"><h2>Appearance</h2><button onclick="document.body.classList.toggle('dark');localStorage.setItem('nicoedu_theme',document.body.className)">Toggle dark mode</button><h2>API endpoint</h2><input id="endpoint" value="${API_URL}"><button onclick="localStorage.setItem('nicoedu_api_url',endpoint.value);toast('Saved — reload the page')">Save endpoint</button></section>`})
