document.addEventListener('DOMContentLoaded',async()=>{if(document.body.dataset.page!=='admin')return;requireAuth();let d=await api('/admin/dashboard'),ss=await api('/admin/students');document.body.innerHTML=`<main class="main"><div class="top"><h1>Admin Dashboard</h1><button onclick="logout()">Logout</button></div><div class="stats">${Object.entries(d).map(x=>`<div class="card stat"><span>${x[0].replaceAll('_',' ')}</span><b>${typeof x[1]==='number'?Math.round(x[1]):x[1]}</b></div>`).join('')}</div><section class="card"><h2>Students</h2><table class="table"><tr><th>Name</th><th>Email</th><th>Status</th><th>Action</th></tr>${ss.map(s=>`<tr><td>${s.first_name} ${s.last_name}</td><td>${s.email}</td><td>${s.is_active?'Active':'Inactive'}</td><td><button onclick="api('/admin/students/${s.user_id}/status?active=${!s.is_active}',{method:'PUT'}).then(()=>location.reload())">${s.is_active?'Deactivate':'Activate'}</button></td></tr>`).join('')}</table></section></main>`})
