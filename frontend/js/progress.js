document.addEventListener('DOMContentLoaded',async()=>{if(document.body.dataset.page!=='progress')return;shell('Progress');const [p,s]=await Promise.all([api('/progress'),api('/progress/subjects')]);content.innerHTML=`<div class="stats"><div class="card stat"><span>Overall progress</span><b>${Math.round(p.overall_progress)}%</b></div><div class="card stat"><span>Average score</span><b>${Math.round(p.average_score)}%</b></div></div><section class="card"><h3>Subject Progress</h3>${s.map(x=>`<p>${x.name} ${Math.round(x.progress)}%</p><div class="bar"><i style="width:${x.progress}%"></i></div>`).join('')}</section>`})
