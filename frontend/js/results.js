document.addEventListener('DOMContentLoaded',async()=>{if(document.body.dataset.page!=='results')return;shell('Quiz Results');let r=await api('/quizzes/'+new URLSearchParams(location.search).get('id')+'/results');content.innerHTML=`<div class="card"><h2>${r.percentage}% — ${r.score} correct</h2>${r.answers.map(a=>`<hr><h3>${a.question_text}</h3><p>Your answer: ${a.selected_answer||'Not answered'} · Correct: ${a.correct_answer}</p><p class="muted">${a.explanation||''}</p>`).join('')}</div>`})
