async function notes(){shell('Notes');const render=async()=>{const ns=await api('/notes');content.innerHTML=`<div class="row"><button onclick="noteForm()">+ Create Note</button><input placeholder="Search notes" oninput="searchNotes(this.value)"></div><div class="grid" id="notes">${ns.map(n=>noteCard(n)).join('')||'<p class="empty">No notes yet.</p>'}</div>`};window.searchNotes=async q=>notes.innerHTML=(await api('/notes?search='+encodeURIComponent(q))).map(noteCard).join('');window.noteForm=()=>modal('Create Note',`<label>Title</label><input name="title" required><label>Content</label><textarea name="content" rows="8" required></textarea><label>Tags</label><input name="tags">`,async d=>{await api('/notes',{method:'POST',body:JSON.stringify(d)});render()});render()}function noteCard(n){return `<article class="card"><h3>${n.title}</h3><p class="muted">${n.subject_name||'General'} · ${n.tags||''}</p><p>${n.content.slice(0,160)}…</p><button class="danger" onclick="api('/notes/${n.note_id}',{method:'DELETE'}).then(()=>location.reload())">Delete</button></article>`}document.addEventListener('DOMContentLoaded',()=>{if(document.body.dataset.page==='notes')notes().catch(x=>toast(x.message))})
